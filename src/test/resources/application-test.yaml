spring:
  datasource:
    url: jdbc:h2:./db/booking-saas;AUTO_SERVER=TRUE
    username: Kanybek
    password: Kanybek12345
    driver-class-name: org.h2.Driver

  h2:
    console:
      enabled: true
      path: /h2-console

  jpa:
    hibernate:
      ddl-auto: create
    database-platform: org.hibernate.dialect.H2Dialect

  flyway:
    enabled: false
